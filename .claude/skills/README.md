# Claude Code Skills

TDD (Red → Green → Refactor) + Tidy First + 설계 워크플로우에 맞춘 슬래시 스킬 모음.

## 스킬 목록

### TDD 사이클

| 스킬          | 설명                    | 인자         |
|-------------|-----------------------|------------|
| `/go`       | plan.md 기반 TDD 자동 수행  | 없음         |
| `/red`      | 실패 테스트 작성             | 요구사항 설명    |
| `/green`    | 테스트 통과 최소 구현          | 없음         |
| `/refactor` | 코드 품질 개선 / Tidy First | 대상/방향 (선택) |

### 커밋

| 스킬        | 설명                        | 인자 |
|-----------|---------------------------|----|
| `/commit` | 검증 보고서 + Tidy First 규칙 커밋 | 없음 |

### 계획 및 설계

| 스킬               | 설명              | 인자            |
|------------------|-----------------|---------------|
| `/plan`          | plan.md 작성/업데이트 | 요구사항/PRD (선택) |
| `/design-review` | 설계 문서 전체 리뷰     | 없음            |

### 설계 산출물 (Mermaid)

| 스킬               | 설명           | 인자     |
|------------------|--------------|--------|
| `/sequence`      | 시퀀스 다이어그램 작성 | 대상 기능  |
| `/class-diagram` | 클래스 다이어그램 작성 | 대상 도메인 |
| `/erd`           | ERD 작성       | 대상 도메인 |

### 요구사항 분석

| 스킬                       | 설명           | 인자      |
|--------------------------|--------------|---------|
| `/requirements-analysis` | 요구사항 → 설계 전환 | 요구사항 설명 |

### 설계 비교

| 스킬                | 설명                | 인자             |
|-------------------|-------------------|----------------|
| `/design-compare` | 설계 문서 간 괴리점 비교 분석 | 디렉토리/파일 (선택)   |

### 테스트 및 문서

| 스킬      | 설명                | 인자          |
|---------|-------------------|-------------|
| `/e2e`  | E2E 테스트 작성        | 대상 API (선택) |
| `/http` | API HTTP 요청 파일 작성 | 대상 API (선택) |

### 결함 수정

| 스킬     | 설명             | 인자    |
|--------|----------------|-------|
| `/fix` | 버그 재현 테스트 → 수정 | 결함 설명 |

---

## 워크플로우 예시

### A. 설계 (요구사항 → 문서)

```
/requirements-analysis          → 요구사항 분석 + Q&A
/sequence 주문 생성 흐름         → 시퀀스 다이어그램
/class-diagram 주문 도메인       → 클래스 다이어그램
/erd 주문 도메인                 → ERD
/design-review                  → 전체 설계 리뷰
/commit                         → 커밋
```

### B. 새 기능 개발 (plan.md 기반 자동)

```
/plan 요구사항 설명              → plan.md 작성
/go → /go → /go → ...          → 항목 하나씩 TDD 소화
/refactor                       → 코드 정리
/commit                         → 커밋
/http                           → HTTP 파일 작성
```

### C. 단계별 수동 TDD

```
/red 요구사항 설명               → 실패 테스트
/green                          → 최소 구현
/refactor                       → 코드 정리
/e2e                            → E2E 테스트
/commit                         → 커밋
```

### D. 결함 수정

```
/fix 결함 설명                   → 재현 테스트 + 수정
/commit                         → 커밋
```

---

## 관련 문서

- 요구사항: `docs/requirements-analysis.md`
- 설계 산출물: `docs/design/`
- (참고) VO 패턴: `docs/value-object.md`
